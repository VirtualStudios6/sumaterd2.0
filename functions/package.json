{
  "name": "sumaterd-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "serve": "npm run build && firebase emulators:start --only functions",
    "seed": "npm run build && node lib/seed.js",
    "grant-admin": "npm run build && node lib/grantAdmin.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "~5.9.2"
  }
}
